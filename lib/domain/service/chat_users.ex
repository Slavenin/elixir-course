defmodule SimpleChat.Domain.Service.ChatUsers do
  def get_user_chats(login) do
    []
  end

  def get_users_in_chat(id) do
    []
  end

  def add_user_to_chat(user) do
    nil
  end

  def remove_user_from_chat(user) do
    nil
  end
end
